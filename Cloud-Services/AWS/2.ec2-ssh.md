# SSH to EC2

```bash
ssh -i `/Downloads/web.pem ubuntu@3.97.9.249  # ubuntu => username, 3.97.9.249 => public IP of the machine

## if any permission error occur?  then run this command to provided permission to access pem file => chmod 400 ~/Downloads/web.pem
```
